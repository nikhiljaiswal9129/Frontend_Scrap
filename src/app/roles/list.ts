export interface list{
    firstName: string;
    lastName: string;
    email: string;
    profileImg: string;
    isAdmin: string;
}

export interface roleList{
    role: string;
    _id: string;
    createdAt: string;
}